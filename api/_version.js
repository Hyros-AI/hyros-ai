/**
 * Template version. Bump on every release of the template (see CHANGELOG.md).
 * Surfaced by /api/health, /api/data and stored in every snapshot as
 * `templateVersion`, so a fork can tell which template it runs.
 */
export const TEMPLATE_VERSION = '0.2.2';
